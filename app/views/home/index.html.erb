<%= form_with url: search_venues_path, method: :get, local: true do |f| %>
  <div class="field">
    <%= f.label :q, "Location" %><br />
    <%= f.text_field :q, value: @location %>
  </div>
  <div class="actions">
    <%= f.submit 'Search' %>
  </div>
<% end %>