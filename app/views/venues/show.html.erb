<h1><%= @venue.name %></h1>
<p><%= @venue.description %></p>

<h2>Offers</h2>
<%= turbo_frame_tag "offers" do %>
  <%= render @offers %>
<% end %>

<%= link_to 'Back to Venues', venues_path, class: 'btn btn-secondary' %>
<%= link_to 'New Offer', new_venue_offer_path(@venue), class: 'btn btn-primary' %>
