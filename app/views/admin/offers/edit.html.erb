<h1>Edit Offer</h1>

<%= form_with(model: @offer, url: admin_offer_path(@offer), local: true) do |form| %>
  <% if @offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@offer.errors.count, "error") %> prohibited this offer from being saved:</h2>
      <ul>
        <% @offer.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :venue_id %>
    <%= form.collection_select :venue_id, Venue.all, :id, :name %>
  </div>

  <div class="actions">
    <%= form.submit 'Update Offer' %>
  </div>
<% end %>

<%= link_to 'Back', admin_offers_path, class: "btn btn-secondary btn-sm" %>

